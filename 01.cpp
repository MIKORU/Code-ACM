#include<cstdio>
#include <cstring>
#include <cmath>
#include <algorithm>
using namespace std;
int n;
int sum;
int s[5005]
int main()
{
    while(~scanf("%d",&n)){
            sum = 0;
        for(int i=0;i<n;i++){
            scanf("%d",&s[i]);
            sum+=s[i];
        }
        sort(s,s+n);
        printf("%d",)
    }
    return 0;
}
