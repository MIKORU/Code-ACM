#include <stdio.h>
#include <string.h>
int main()
{
    char pre;
    while(scanf("%c",a)!=EOF)
    {
        int fis = 1;
        if(a==13) continue;
        if(fis==1)
        {
            pre = a;
            fis = 0;
        }

    }
    return 0;
}
